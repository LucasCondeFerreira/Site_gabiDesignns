import type { NextPage } from "next";
import Range from "@components/esthetic/range/Range";

const Home: NextPage = () => {
  return (
    <div>
      <Range />
    </div>
  );
};

export default Home;
